<?php
require("../config.php");

/*$hostname=$hostname;
$db_login=$username;
$db_pass=$password;

// new database name
$database =$database;

// Initial UserName and Password
   $ad_login = "Admin";
	$ad_pass = "Admin";
	
// connects to host
$con = mysql_connect($hostname, $db_login, $db_pass) or die("Could not connect");   
 mysql_select_db($database, $con) or die('Could notT select Database');*/

   ?>